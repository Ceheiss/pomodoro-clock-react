import React from 'react';

const StartStopButton = () => {
  return (
    <button id="start_stop" onClick={() => console.log('start-stops')}>
      START-STOP
    </button>
  );
};

export default StartStopButton;
